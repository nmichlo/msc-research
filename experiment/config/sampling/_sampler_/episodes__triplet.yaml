name: episodes__triplet

sampler_cls:
  _target_: s12045.dataset.sampling.RandomEpisodeSampler
  num_samples: 3
  # TODO: this needs to be updated to use the same API as ground_truth wrappers.
  sample_radius: 32
